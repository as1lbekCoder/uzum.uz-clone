import { Carousel, CarouselContent, CarouselItem } from "./ui/carousel"

const Hero = () => {
    return (
        <main>
            <section>
                <Carousel>
                    <CarouselContent>
                        {}
                    </CarouselContent>
                </Carousel>
            </section>
            <section>

            </section>
        </main>
    )
}

export default Hero